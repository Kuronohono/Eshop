import React, {useCallback, useEffect, useState, useRef, } from 'react'



const PriceRangeSlider = ({
    min,
    max,
    trackingColor = "black",
    onChange,
    rangeColor = "#ff0303",
    valueStyle = "price_slider",
    width = "300px",
    currencyText = "$"
}) => {

    const [minVal, setMinVal] = useState(min);
    const [minVal, setMinVal] = useState(min);
  return (
    <div>PriceRangeSlider</div>
  )
}

export default PriceRangeSlider